# Regras do Workspace (OpenClaude)

Ao iniciar uma nova sessão neste workspace (inclusive após fechar e abrir o VS Code), siga esta ordem obrigatória antes de qualquer resposta:

1. Ler `AI/agente.md` por completo.
2. Aplicar integralmente as regras definidas em `AI/agente.md`.
3. Confirmar internamente que as regras estão ativas para toda a sessão atual.

Regras adicionais:
- Sempre tratar `AI/agente.md` como fonte de instrução prioritária neste projeto.
- Nunca iniciar execução de tarefas sem antes carregar esse arquivo.
